{
  "name": "onemap-server",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "export NODE_ENV=development && nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "debug": "^4.1.1",
    "diskusage": "^1.1.3",
    "drivelist": "^8.0.6",
    "express": "^4.16.4",
    "fs-extra": "^8.1.0",
    "ps-tree": "^1.2.0",
    "rosnodejs": "^3.0.0",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
